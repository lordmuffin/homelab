apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cert-manager-technitium-webhook
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: networking
  source:
    repoURL: https://kittizz.github.io/cert-manager-technitium-webhook
    chart: cert-manager-technitium-webhook
    targetRevision: cert-manager-technitium-webhook-1.0.0
    helm:
      releaseName: cert-manager-technitium-webhook
      values:
  destination:
    namespace: cert-manager
    name: in-cluster
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
    - Validate=false
    - CreateNamespace=true
    - PrunePropagationPolicy=foreground
    - PruneLast=true
    - ApplyOutOfSyncOnly=false
    - Prune=true
    - Replace=true
    retry:
      limit: 10
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 60m
