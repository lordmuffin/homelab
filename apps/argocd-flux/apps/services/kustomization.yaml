apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - kasm-app.yaml
  - homepage-app.yaml
  # Missing files - add when ready:
  # - gitea-app.yaml
  # - n8n-app.yaml
  # - media/
  # - mlops/

namespace: flux-system

commonLabels:
  app.kubernetes.io/component: services-application
  category: services