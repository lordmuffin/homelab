# Paperless-ngx Application converted from ArgoCD to FluxCD
# Original: base/services/paperless.yaml
apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: paperless
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./apps/services/paperless
  prune: true
  sourceRef:
    kind: GitRepository
    name: homelab-main
    namespace: flux-system
  targetNamespace: services
  
  # Health checks
  healthChecks:
    - apiVersion: apps/v1
      kind: Deployment
      name: paperless
      namespace: services
  
  # Converted from ArgoCD sync policy
  retryInterval: 2m0s
  timeout: 15m0s
  wait: true