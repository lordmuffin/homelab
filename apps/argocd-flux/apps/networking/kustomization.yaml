apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - traefik-app.yaml
  # Missing files - add when ready:
  # - cert-manager-app.yaml
  # - cilium-app.yaml
  # - headscale-app.yaml
  # - external-dns/

namespace: flux-system

commonLabels:
  app.kubernetes.io/component: networking-application
  category: networking