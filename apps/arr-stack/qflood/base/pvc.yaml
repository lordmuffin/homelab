apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qflood-data
  namespace: qflood
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: freenas-nfs-csi
