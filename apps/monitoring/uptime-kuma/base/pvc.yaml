apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kuma-storage-pvc
  namespace: monitoring
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1000Gi
  storageClassName: sata-large
