apiVersion: v1
kind: Service
metadata:
  name: beszel-internal
  labels:
    app: beszel
  annotations:
    kube-vip.io/vipHost: beszel
spec:
  ports:
    - name: web
      port: 80
      targetPort: 8090
      protocol: TCP
  selector:
    app: beszel
  type: ClusterIP