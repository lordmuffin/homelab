apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: metabase-storage
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: freenas-iscsi-csi
  resources:
    requests:
      storage: 5Gi
