apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: beszel-storage
  namespace: beszel
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: sata
  resources:
    requests:
      storage: 5Gi
