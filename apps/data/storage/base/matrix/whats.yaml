apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: whats-bridge-data
  namespace: matrix1
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: freenas-iscsi-csi
  resources:
    requests:
      storage: 1Gi
