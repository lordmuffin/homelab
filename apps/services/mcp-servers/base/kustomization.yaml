apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mcp-servers

resources:
  - namespace.yaml
  - rbac.yaml
  - shared-config.yaml
  - monitoring.yaml

commonLabels:
  app.kubernetes.io/part-of: mcp-servers
  app.kubernetes.io/component: base-infrastructure