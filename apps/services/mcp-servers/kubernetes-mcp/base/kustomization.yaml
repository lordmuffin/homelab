apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mcp-servers

resources:
  - deployment.yaml
  - svc.yaml
  - configmap.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/name: kubernetes-mcp
  app.kubernetes.io/component: mcp-server
  app.kubernetes.io/instance: kubernetes-mcp