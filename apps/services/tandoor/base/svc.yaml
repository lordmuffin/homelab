apiVersion: v1
kind: Service
metadata:
  name: tandoor-internal
  labels:
    app: tandoor
  annotations:
    kube-vip.io/vipHost: tandoor
spec:
  ports:
    - name: web
      port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: tandoor
  type: ClusterIP