apiVersion: v1
kind: Service
metadata:
  name: litellm
  namespace: services
  labels:
    app: litellm
spec:
  selector:
    app: litellm
  ports:
    - name: http
      port: 80 # Service port
      targetPort: http # Target port name from deployment (4000)
      protocol: TCP
  type: ClusterIP