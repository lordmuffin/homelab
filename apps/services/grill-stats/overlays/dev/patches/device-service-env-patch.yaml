apiVersion: apps/v1
kind: Deployment
metadata:
  name: device-service
  namespace: grill-stats
spec:
  template:
    spec:
      containers:
      - name: device-service
        env:
        # ThermoWorks Configuration
        - name: THERMOWORKS_CLIENT_ID
          valueFrom:
            secretKeyRef:
              name: grill-stats-dev-creds-1password
              key: THERMOWORKS_API_KEY
        - name: THERMOWORKS_CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              name: grill-stats-dev-creds-1password
              key: SECRET_KEY
        - name: THERMOWORKS_REDIRECT_URI
          value: "http://localhost:8080/api/auth/thermoworks/callback"
        - name: THERMOWORKS_POLLING_INTERVAL
          value: "60"
        
        # Home Assistant Configuration
        - name: HOMEASSISTANT_URL
          value: "http://homeassistant.home.svc.cluster.local:8123"
        - name: HOMEASSISTANT_TOKEN
          valueFrom:
            secretKeyRef:
              name: grill-stats-dev-creds-1password
              key: HOMEASSISTANT_TOKEN
        
        # Database Configuration
        - name: DATABASE_URL
          value: postgresql://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@grill-stats-db.grill-stats-dev.svc.cluster.local:5432/grillstats
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: grill-stats-dev-creds-1password
              key: DB_USERNAME
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: grill-stats-dev-creds-1password
              key: DB_PASSWORD
        
        # Redis Configuration
        - name: REDIS_URL
          value: redis://:$(REDIS_PASSWORD)@redis.grill-stats-dev.svc.cluster.local:6379/0
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: grill-stats-dev-creds-1password
              key: REDIS_PASSWORD
        
        # RFX Gateway Configuration
        - name: RFX_SCAN_DURATION
          value: "30"
        - name: RFX_CONNECTION_TIMEOUT
          value: "15"
        - name: RFX_SETUP_TIMEOUT
          value: "300"