# Azure DevOps Template: Terraform Setup
# Common setup steps for Terraform operations

parameters:
- name: azureServiceConnection
  type: string
  default: 'azure-terraform-backend'
- name: terraformVersion
  type: string
  default: '1.5.7'
- name: workingDirectory
  type: string
  default: 'terraform'
- name: environment
  type: string
- name: module
  type: string
- name: stateKey
  type: string

steps:
- task: AzureCLI@2
  displayName: 'Azure CLI Login'
  inputs:
    azureSubscription: ${{ parameters.azureServiceConnection }}
    scriptType: 'bash'
    scriptLocation: 'inlineScript'
    inlineScript: |
      echo "✅ Azure CLI authenticated successfully"
      az account show --output table

- task: TerraformInstaller@0
  displayName: 'Install Terraform'
  inputs:
    terraformVersion: ${{ parameters.terraformVersion }}

- task: PowerShell@2
  displayName: 'Verify Terraform Installation'
  inputs:
    targetType: 'inline'
    script: |
      Write-Host "Terraform version:"
      terraform version
      
      Write-Host "Environment: ${{ parameters.environment }}"
      Write-Host "Module: ${{ parameters.module }}"
      Write-Host "Working Directory: ${{ parameters.workingDirectory }}"
      Write-Host "State Key: ${{ parameters.stateKey }}"

- task: PowerShell@2
  displayName: 'Configure Terraform Backend'
  inputs:
    targetType: 'inline'
    workingDirectory: ${{ parameters.workingDirectory }}
    script: |
      # Create backend configuration for Azure Storage
      $backendConfig = @"
      terraform {
        backend "azurerm" {
          resource_group_name  = "$(AZURE_BACKEND_RESOURCE_GROUP)"
          storage_account_name = "$(AZURE_BACKEND_STORAGE_ACCOUNT)"
          container_name       = "$(AZURE_BACKEND_CONTAINER_NAME)"
          key                  = "${{ parameters.stateKey }}"
        }
      }
      "@
      
      # Write backend configuration to file
      $backendConfig | Out-File -FilePath "backend.tf" -Encoding UTF8
      
      Write-Host "✅ Created backend configuration for ${{ parameters.environment }}/${{ parameters.module }}"
      Write-Host "Backend configuration:"
      Get-Content "backend.tf"